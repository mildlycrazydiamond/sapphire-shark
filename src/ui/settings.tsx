import Box from "@mui/material/Box";

export default function Settings() {
    return (
        <Box
      sx={{ flexShrink: 1, bgcolor: 'background.paper', display: 'flex', height: 480, width: 320 }}
    >
            <span>Settings</span>
        </Box>
    );
}